[project]
name = "python-app"
version = "0.1.0"
description = "Hello World Python demo"
requires-python = ">=3.13"

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
