[tools]
java = "25"
gradle = "latest"

[tasks.setup]
description = "Download Java/Gradle dependencies"
run = "gradle dependencies --quiet"

[tasks.build]
description = "Build the Java project"
run = "gradle build"

[tasks.test]
description = "Test the Java project"
run = "gradle test"

[tasks.run]
description = "Run the Java project"
run = "gradle run -q"
